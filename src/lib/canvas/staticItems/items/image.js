import  uuid  from '../../../uuid';

// add type in data
// remove item in draw
// change class name
export default class ImageObject {
  
  static data() { 
    return {

    };
  }

  
  static draw(ctx, itemExtra,assets) {

  }
}